// Tailwind v4 postcss plugin per Vercel + Tailwind guidance.
// Do NOT use the "tailwindcss" plugin name (that is the v3 path).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
