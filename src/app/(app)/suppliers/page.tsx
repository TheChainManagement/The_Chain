import { BenchStub } from '@/components/bench/BenchStub';

export const metadata = { title: 'Suppliers · The Chain' };

export default function SuppliersPage() {
  return (
    <BenchStub
      eyebrow="Reliability you can defend"
      title="Suppliers"
      prefix="Suppliers"
      message="On-time-in-full scorecards, lead-time variance, and reliability rollups arrive with your first received POs."
    />
  );
}
