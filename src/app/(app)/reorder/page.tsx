import { BenchStub } from '@/components/bench/BenchStub';

export const metadata = { title: 'Reorder · The Chain' };

export default function ReorderPage() {
  return (
    <BenchStub
      eyebrow="Reorder points you can defend"
      title="Reorder"
      prefix="Reorder queue"
      message="Safety stock, reorder points, and recommended order quantities surface here, each with the reasoning behind it."
    />
  );
}
