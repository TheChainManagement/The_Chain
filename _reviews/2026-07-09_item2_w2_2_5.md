# Codex Review — item2_w2_2_5
**Date:** 2026-07-09 20:02
**Project:** The Chain
**Project type:** MoreTech Product
**Phase:** 6 (Features)
**Unit reviewed:** item2_w2_2_5
**Review weight:** full
**Skills audited:** none
**Reviewer:** Codex CLI (model: gpt-5.4)

---

## What was done

- The W2-2.5 schema work is real. `supabase/migrations/20260709210000_w2_2_5a_hold_release_enum.sql` adds `hold` / `release`, and `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:31-67` adds `purchase_uom`, `purchase_to_stock_factor`, `avg_unit_cost`, `avg_cost_provenance`, and `on_hold`, plus the related checks.
- The posting-kernel function exists on disk. `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:93-214` defines `post_stock_movement()` with sign checks, demand-ref enforcement, hold/release guards, receipt cost averaging, ledger insert, and balance update under row lock.
- The inventory valuation surface exists. `src/app/(app)/inventory/ValuationStrip.tsx:15-48` renders the strip and export link, and `src/app/api/exports/inventory/valuation/route.ts:11-28` serves the CSV export.
- The hold/release UI exists. `src/app/(app)/inventory/OperatorPanel.tsx:350-493` adds the Hold/Release form, and `tests/inventory/operator-panel-hold.test.tsx:57-151` exercises the panel behavior.
- The receive conversion UI exists. `tests/purchase-orders/receive-conversion.memorable.test.tsx:55-83` proves the conversion rail behavior in component tests, and `tests/inventory/kernel.test.ts:285-364` covers purchase-UoM approval/receipt/convert behavior against the local schema.
- The system contract was updated in part. `SYSTEM_DESIGN.md:169-175` documents `on_hold`, `avg_unit_cost`, and purchase-UoM supplier links, and `SYSTEM_DESIGN.md:358-370` adds the new “Inventory posting kernel” section.

## What wasn't done

- The claimed “every balance mutation flows through the one posting service” is false. `docs/NEXT_SESSION_KICKOFF_PROMPT.md:199-202` makes that an acceptance criterion, `_reviews/2026-07-09_item2_w2_2_5_evidence.md:26-46` claims it, but `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:449-463` still has `apply_po_approval()` writing `inventory_levels.in_transit` directly instead of going through `post_stock_movement()`.
- The required memorable artifact was not delivered in the required place or shape. `MASTER_PROMPT.md:21` and `MASTER_PROMPT.md:134-143` require `_reviews/<date>_feature_<name>_memorable.{png,test.ts}` plus `_reviews/<date>_feature_<name>.md`. The evidence points to `tests/purchase-orders/receive-conversion.memorable.test.tsx` and `tests/inventory/operator-panel-hold.test.tsx` at `_reviews/2026-07-09_item2_w2_2_5_evidence.md:69-75`, but there is no matching `_reviews/2026-07-09_feature_*_memorable.*` artifact on disk, and the only review artifact present is `_reviews/2026-07-09_item2_w2_2_5_evidence.md`.
- The claimed Codex review artifact does not exist. The evidence says `Codex review: _reviews/2026-07-09_item2_w2_2_5.md` at `_reviews/2026-07-09_item2_w2_2_5_evidence.md:111-113`, but that file is not on disk.
- There is no `FEATURES.md` block for this slice. `MASTER_PROMPT.md:134` says done status depends on acceptance criteria from `FEATURES.md`, but this item is driven from `docs/NEXT_SESSION_KICKOFF_PROMPT.md` instead. `rg` finds no `Item 2`, `W2-2.5`, or `inventory-core hardening` entry in `FEATURES.md`.

## What can be done better

- Stop overstating the kernel refactor. The migration header says “every balance mutation flows through here” at `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:208-214`, the evidence repeats it at `_reviews/2026-07-09_item2_w2_2_5_evidence.md:26-46`, and the code immediately breaks that promise in `apply_po_approval()` at `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:449-463`. That kind of contract drift is exactly how future procurement work gets built on a lie.
- The review/evidence trail is sloppy. The slice is using an ad hoc item-named evidence file and test files under `tests/` instead of the project’s required `_reviews/...` feature artifact convention from `MASTER_PROMPT.md:21` and `:143`. This repo already has enough review-artifact drift; adding another one is undisciplined.
- The “exactly one shared helper” story is weaker than advertised. The real engine paths do use `netPosition`, but `src/lib/policy/compute.ts:155` still documents the old formula as `on_hand + in_transit − allocated`. The implementation is better than the comment; the comment is now wrong and will mislead the next edit.
- The proof is too component-heavy for a feature that changes inventory value, receiving behavior, and hold flows on the main bench. The memorable tests in `tests/purchase-orders/receive-conversion.memorable.test.tsx` and `tests/inventory/operator-panel-hold.test.tsx` are jsdom component tests, not browser-level evidence of the actual page flow the contract keeps demanding.

## What was missed

- `SYSTEM_DESIGN.md` is still internally stale. The new kernel section exists, but the table definition for `inventory_op_events` still says the enum is only `issue/adjustment/cycle_count_close` at `SYSTEM_DESIGN.md:171`, while the migration adds `hold` at `supabase/migrations/20260709210100_w2_2_5b_inventory_core.sql:64-67`.
- The production-ready gate items were not evidenced. I found no on-disk proof for WCAG 2.1 AA, Lighthouse ≥ 85, or a per-feature review artifact for this slice, despite `MASTER_PROMPT.md:134-143` making those part of “done.”
- The feature-contract discipline was skipped. This project’s own rule is “each `FEATURES.md` block names its memorable element” at `MASTER_PROMPT.md:21`, but this slice has no corresponding `FEATURES.md` block at all, so there is no canonical feature contract for Phase 6 to review against. That is process drift, not a harmless shortcut.

## Decisions (captured 2026-07-09, round-1 triage by Claude; MG confirms at the checkpoint)

### "Every balance mutation flows through the kernel" is overstated (apply_po_approval writes in_transit directly)
- **Decision:** Fix the CLAIM, not the code — deliberate design: the in_transit commitment is not a ledger event (no goods moved), so it stays in `apply_po_approval`, which lives in the kernel migration as a documented kernel-surface function; the kernel receipt drains it via `affects_in_transit`.
- **Action:** Precise wording landed in the migration header + kernel COMMENT, SYSTEM_DESIGN kernel section ("every LEDGER-BACKED balance"), and the evidence file. MG may override to "route it through the kernel anyway" at the checkpoint.

### Memorable artifact not at the `_reviews/<date>_feature_*_memorable.*` path
- **Decision:** Fix now (follow the Item-1 / audit-chain precedent).
- **Action:** Copied to `_reviews/2026-07-09_feature_item2_w2_2_5_memorable.test.tsx` (runnable original stays in tests/ where vitest includes it).

### "Codex review artifact does not exist"
- **Decision:** Chicken-and-egg — the evidence file referenced this review's path before the review ran. It exists now (this file).
- **Action:** None needed.

### No FEATURES.md block for W2-2.5 (Wave-2 items driven from the kickoff doc)
- **Decision for MG:** Items 0-2 have all used `docs/NEXT_SESSION_KICKOFF_PROMPT.md` as the feature contract (FEATURES.md is the Wave-1 block ledger). Options: (a) accept kickoff-doc-as-contract for Wave 2 and say so in MASTER_PROMPT, (b) backfill FEATURES.md blocks for W2 items, (c) leave as-is.
- **Action:** Awaiting MG at the checkpoint.

### Stale `on_hand + in_transit − allocated` comment in policy/compute.ts
- **Decision:** Fix now.
- **Action:** Comment corrected to the netPosition formula.

### SYSTEM_DESIGN inventory_op_events kind enum missing 'hold'
- **Decision:** Fix now.
- **Action:** Updated.

### Proof "too component-heavy"; WCAG/Lighthouse not evidenced
- **Decision:** Partially factual — the slice WAS live-verified in a real browser this session (strip, hold post, held tag, CSV export, numbers recorded in the evidence file); Codex cannot see preview sessions. Browser-level Playwright page-flow tests + Lighthouse remain open: Lighthouse ≥ 90 already has a standing Preview-environment ticket; WCAG spot-checks (role=status live regions, focus-visible global rule) hold but no formal audit ran.
- **Action:** Ticketed a Playwright page-flow pass for /inventory hold + receive rail alongside the standing Lighthouse ticket. MG may reprioritize.
