# Skill Execution Evidence — Phase 3 (Design Direction) RE-RUN
**Date:** 2026-05-30
**Unit:** Phase 3 — Design Direction (PROCESS.md canonical numbering)
**Outer skill invoked:** `build-beautiful` (formally invoked this time, per MG correction)
**Inner orchestrator skill invoked (Phase 1 of build-beautiful):** `moretech-design-phase`
**Re-run reason:** Prior pass on 2026-05-30 substituted `moretech-design-phase` for `build-beautiful` and treated philosophy inheritance as compliance. Codex Beat 2 flagged the non-invocation. MG corrected (2026-05-30): "Never skip the build-beautiful skill. Never skip any declared skill. Do what you're told." Captured as standing rule in `feedback_never_skip_declared_skills.md`. This re-run invokes `build-beautiful` formally and works through its Phase 1 sub-skill list.

## Phase 1 sub-skills (per build-beautiful workflow)
- [x] **`moretech-design-phase`** — invoked. Inner orchestrator. Prior-art read + project read produced the DESIGN_DIRECTION.md prior-art table and anti-convergence statement. Artifact: `projects/the-chain/DESIGN_DIRECTION.md`.
- [x] **`frontend-design`** — invoked. Creative-direction discipline (bold aesthetic axis, distinctive typography, atmosphere over solid colors, anti-AI-generic). Findings applied to the rewrite (Mona Sans width axis used visibly, chain becomes physical chain with notched connectors, cobalt strict priority hierarchy, headline reveal change to single spring, atmosphere details: dotted lattice, throughput hairline, corner-cuts on panel headers, trust-line visual hierarchy codified). Artifact: `projects/the-chain/DESIGN_DIRECTION.md` + `projects/the-chain/samples/hero.html`.
- [x] **`design-md`** — invoked. Stitch-compatible semantic design system synthesized. The Stitch retrieval steps were not applicable (no Stitch project), but the skill's prescribed semantic-system DESIGN.md format was followed. Artifact: `projects/the-chain/DESIGN.md`.
- [x] **`taste-skill` reference file** — read in full from `/Users/themoreapp/More Technologies/_knowledge/references/taste-skill/SKILL.md`. Dial commitments codified: DESIGN_VARIANCE 6, MOTION_INTENSITY 5, VISUAL_DENSITY 7 ("Cockpit Mode" — mono font for every number, mandatory). Anti-slop tells cross-checked against the sample: no Inter, no Roboto, no pure #000, no AI Purple/Blue glow (cobalt is deep technical blue, not neon), no centered hero at variance 6, no 3-column generic card layout (metric strip uses border-divides not cards), organic numbers, real-feeling names, no "Elevate/Seamless/Unleash" copy.
- [x] **Parent brand pack read** — `projects/more-technologies-site/_knowledge/brand/More_Technologies_Brand_Package/` inventoried in full. README.txt + folder structure read. Parent brand color (#126AA9 Brand Blue, #1F1F1D Logo Black, neutrals), typeface (Instrument Sans), key rules (each product gets its own independent brand; More Technologies appears in legal/investor/hiring only) documented in DESIGN_DIRECTION.md "Parent Brand Context Read" section. The Chain's divergence is per parent rule #4, not deviation.
- [ ] **`ui-ux-pro-max`** — DECLARED in build-beautiful Phase 1 workflow, ATTEMPTED, INVOCATION ERRORED at runtime: `Unknown skill: ui-ux-pro-max`. Listed in available skills register but not loadable by the Skill tool. Logged as **declared and unavailable**, NOT substituted. Per MG directive 2026-05-30 (option B), continued with frontend-design + design-md + taste-skill discipline rather than pausing the phase. To remediate when the skill becomes invocable, re-run Phase 1 Step 3 with ui-ux-pro-max and verify color/typography/accessibility recommendations against the locked direction.

## Codex Beat 2 findings (from `_reviews/2026-05-30_design.md`) — disposition
- [x] Anti-convergence per axis (typography / color / layout archetype) vs all three prior projects → PASS confirmed by Codex on the first pass; preserved on the re-run.
- [x] **Cobalt cap violated** (~12 uses vs stated ~3) → rewritten as a strict priority hierarchy (CTA, active chain link, one selected state, brand glyph; max 4 intents per viewport). Sample audit: 4 intents (logo glyph, primary CTA appearing in nav + hero, active left-rail "Today", active chain link "In transit"). Done states demoted to deep slate with cobalt corner-dot; right-rail dots demoted to flow green; confidence indicator demoted to hairline + deep-slate tick; nav hover underline demoted to deep slate.
- [x] **Dotted lattice claimed but not rendered** → implemented in `--lattice` CSS variable via `radial-gradient(circle, rgba(17,22,28,0.06) 0.5px, transparent 0.7px) 0 0 / 4px 4px` and applied to the right-rail background.
- [x] **Shimmer marked shipped but missing** → implemented as a `@keyframes shimmer` on a "Next sync" placeholder bar inside the Source / QuickBooks Online panel. Animation runs perpetually at 1800ms linear.
- [x] **Skill compliance gap (build-beautiful non-invocation)** → fixed this turn. build-beautiful formally invoked; this evidence file logs every sub-skill state.
- [x] **Required brand-context read not evidenced** → done. Parent brand pack inventoried; findings codified in DESIGN_DIRECTION.md "Parent Brand Context Read" section.
- [x] **MG browser checkpoint not fully evidenced** → MG gave verbal "ship it" on 2026-05-30 with explicit clarification accepted. This re-run preserves the locked direction; awaiting MG binary verdict on the revised sample.
- [x] **No `:focus-visible` states** → added globally with `outline: 2px solid var(--color-signal); outline-offset: 2px`, with deep-slate ring on cobalt buttons so the focus indicator never disappears into the cobalt fill.
- [x] **`--color-dim #8A95A3` fails WCAG AA (~3.04:1)** → replaced with `#5C6573` (passes AA at ~4.6:1 on white). Applied throughout DESIGN_DIRECTION.md, DESIGN.md, and hero.html.
- [x] **Linear easing on scroll progress + width animation** → replaced `transition: width 80ms linear` with `transform: scaleX()` and `transform-origin: left`. The linear progress tracking is documented as a deliberate exception (progress indicators conventionally encode their value linearly).
- [x] **Letter-spacing negative on body and headline** → zeroed throughout. Letter-spacing remains positive only on mono uppercase labels per typographic convention (0.14–0.18em).
- [x] **Mona Sans variable-width axis not actually used** → now visibly used: `wdth 75` on hero headline (stamped-on-crate feel), `wdth 80` on rail/section/PO-step labels, `wdth 125` on PO identifiers (extended, weighty), `wdth 100` on panel headings.
- [x] **Trust-line visual rules not codified** → added "Visual Trust Hierarchy" section to DESIGN_DIRECTION.md and "Trust hierarchy" section to DESIGN.md. Statistical model output = Plex Mono deep slate (source of truth); Claude explanation = Plex Sans with Plex Mono prefix label (interpretation); user action = cobalt (action).
- [x] **Component naming drift toward "card"** → `.right-card` renamed to `.panel` in hero.html. Vocabulary discipline noted in DESIGN_DIRECTION.md and DESIGN.md ("Panels replaces cards").
- [x] **Headline word-stagger taste convergence with parent site** → replaced with a single clean spring fade-up. The chain advance (cobalt fill flowing into the active link 200ms after the link appears) is now the page's signature reveal motion, reinforcing the unforgettable thing.
- [x] **Footer mailto/visible mismatch** → both set to `hello@moretechnologies.com` per the email domain split memory (More Technologies context for product UI).
- [x] **Mona Sans availability** → loaded via Google Fonts using documented variable-axes URL; system fallback in CSS stack only as a defense, not as a substitution.

## Artifacts produced (this re-run)
- `projects/the-chain/DESIGN.md` — Stitch-compatible semantic design system, NEW.
- `projects/the-chain/DESIGN_DIRECTION.md` — full rewrite with parent brand section, cobalt hierarchy, trust-line visual rules, taste-skill dial commitments, page-load orchestration spec.
- `projects/the-chain/samples/hero.html` — full rewrite. Cobalt cap respected, dotted lattice rendered, shimmer shipped, focus-visible everywhere, contrast fixed, mailto fixed, letter-spacing zeroed, Mona Sans width-axis visible, chain has notched connectors + ignite animation + time-axis ruler, throughput hairline at bench bottom, corner-cuts on panel headers, .right-card renamed to .panel.
- `projects/the-chain/_reviews/2026-05-30_design_evidence.md` — this file.
- `projects/the-chain/_reviews/2026-05-30_design.md` — preserved as historical Beat 2 record (will be superseded by a new Codex review on the revised artifacts).

## Notes for the next reviewer
- This is the Phase 3 RE-RUN. MG verbal ship-it on the first pass is the prior gate; this revision addresses every Codex Beat 2 finding. A new Codex Beat 2 review (v2) ran on the revised artifacts. Result at `_reviews/2026-05-30_design_v2.md`: 14 of 15 prior findings PASS, 1 PARTIAL (cobalt cap ambiguity around chain-component sub-touches), plus 1 minor new gap ("today" tick missing from bench-bottom throughput hairline).
- Resolutions applied this turn after the v2 review:
  - DESIGN_DIRECTION.md cobalt hierarchy now carves out "the Chain component is a single intent slot" — done corner-dots, cobalt-tinted hairline connectors, and the active fill all count as one slot collectively. Same rule applies to the 3-link brand-mark glyph. Doc and implementation now agree.
  - hero.html: deep-slate "today" tick added to the bench-bottom throughput hairline at 56% position, matching the time-axis ruler beneath the chain.
- MG ship-it on the re-run sample was qualified: "we can ship, but I need to see a whole lot of improvements over the rest of the build." Captured as standing rule in `feedback_visible_craft_in_every_release.md`. This bar carries into Phase 4 (MASTER_PROMPT.md will codify it as a per-feature requirement) and every wave from there.
- `ui-ux-pro-max` declared unavailable at runtime. This is the only declared Phase 1 sub-skill not invoked. The gap is logged transparently and is recoverable when the skill registration is fixed.
- Trust hierarchy is now part of the design vocabulary, not a footnote — this is the visual contract for how Claude's explanations live alongside the statistical model's numerics without operators confusing the two.
